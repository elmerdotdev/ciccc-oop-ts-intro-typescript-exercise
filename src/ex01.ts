// 1. Create a function named `multiply` that takes two parameters: both numbers.
//    The function should return the product of the two numbers.
//    Ensure the parameters and return type are typed.

function multiply(a, b) {
  
}

// Expected output:
console.log(multiply(3, 5)); // 15
console.log(multiply(7, 2)); // 14